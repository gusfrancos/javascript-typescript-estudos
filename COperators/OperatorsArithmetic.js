/* Operadores aritmeticos
        + (Adição / contatenação)
        - (subtração)
        / (divisão)
        * (multiplicação)
        ** (potenciação)
        % (resto da divisão)

        ++ (operador de incremento)
        -- (operador de decremento)
*/
